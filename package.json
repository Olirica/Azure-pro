{
  "name": "azure-pro",
  "version": "1.0.0",
  "description": "End-to-end Azure speech pipeline with ASR, translation, and TTS.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "cross-env NODE_ENV=development node server/index.js",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "prestart": "npm --prefix client install && npm --prefix client run build",
    "lint": "node scripts/patch-fuzzer.js --lint-check",
    "test": "node scripts/wav-harness.js --self-test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@deepgram/sdk": "^4.11.3",
    "axios": "^1.13.2",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "ioredis": "^5.9.1",
    "microsoft-cognitiveservices-speech-sdk": "^1.47.0",
    "pg": "^8.12.0",
    "pino": "^10.1.1",
    "prom-client": "^15.1.3",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "cross-env": "^7.0.3",
    "playwright": "^1.57.0"
  }
}
